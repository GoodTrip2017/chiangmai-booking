# 安全性檢測報告

初次檢測：2026-09-14；部署補驗與執行環境複查：2026-09-21。對象：本機修正版及正式 Railway 服務。

**本機 46 組測試通過；2026-09-14 修正版正式 HTTPS 檢查 31 項與業務／登入檢查 14 項通過。2026-09-21 npm audit 0 項已知漏洞。精簡執行映像已在 CI 與 Railway 建置成功並正式上線，31 項公開 HTTPS 再驗全數通過；寄信憑證及實際收件已驗證，原生持續備份已啟用；高流量防護仍有下列限制。** 本報告不是第三方滲透測試認證，也不把尚未部署的設定列為通過。

## 實際做了什麼

讀查全部應用程式、前後台腳本、資料庫操作、通知服務、啟動及部署設定；在隔離的 PostgreSQL 測試 schema 上送出正常與惡意 HTTP 請求。另啟動獨立 Node.js 程序，測試多實例與程序重啟；使用本機模擬 PostgreSQL 協議端點完成真正的 TLS 握手；用真正瀏覽器登入、檢查惡意資料呈現及登出。

環境為 macOS、Node.js 24.14.0、npm 11.9.0、臨時 PostgreSQL 18.4。沒有使用正式客戶資料，沒有寄出 Email 或 LINE 訊息。套件版本依 `package-lock.json`；受測程式指紋見 [source-manifest.json](security-evidence/source-manifest.json)。

## 發現並修正

| 問題 | 實際影響與成立條件 | 修正與驗證 |
|---|---|---|
| 舊預約請求重送 | 持有原請求鍵與原表單內容的人，在店員修改後可能讀到修改後的姓名／Email；取消後仍可能看到成功 | 資料或狀態變更即回 409，不揭露修改內容，也不新增另一筆；SEC-08 |
| 舊資料來源欄位直接組成 HTML | 新 API 已限制來源，但匯入或舊資料中的 HTML 仍可能改變後台畫面；原 CSP 是額外防線，不能取代跳脫 | 補上輸出跳脫；SEC-09 與實際瀏覽器檢查均通過 |
| LINE 異常事件可能終止程序 | 必須具有有效簽章；`events` 若不是陣列，原本會產生未處理的非同步例外 | 驗證陣列、事件格式與數量，統一錯誤處理；獨立程序收到異常事件後仍能回應健康檢查；SEC-11 |
| 欄位型別及查詢參數過寬 | 陣列、布林值可能被轉成合法姓名、人數或日期；控制字元可能進入資料庫；UUID 陣列可能造成錯誤 | 明確檢查型別、有限整數、控制字元、Email 及 UUID；重複／陣列日期拒絕；SEC-06、07 |
| 後台大小寫路徑未套用禁止快取 | `/ADMIN` 能載入後台，但原先只對小寫路徑設定 no-store | 統一路徑檢查；未知頁面也使用一致安全標頭；SEC-04 |
| 預約防灌單只存在記憶體 | 重啟或切換服務實例會重設限制 | 改為資料庫原子計數；3 次／分鐘、20 次／天；真實程序重啟及兩實例競爭驗證；SEC-13 |
| IPv6 地址切換及密碼運算成本 | 同一 IPv6 網段可能使用不同地址增加嘗試；原 scrypt 使用較低預設成本 | IPv6 以 /56 合併、密碼雜湊參數明確升級，每實例同時最多 2 次密碼驗證；SEC-12、14、17 及單元測試 |
| 連續重寄信缺乏限制 | 已登入的人可能重複觸發同一封通知 | 同筆每分鐘最多 1 次，全站每小時 60 次；8 個同時請求僅 1 個呼叫寄信服務；SEC-15 |
| 資料庫 URL 可覆蓋 SSL 設定 | 管理者混用 `sslmode=no-verify` 等參數時，pg 會覆蓋程式指定的憑證驗證或 CA | 拒絕混用，統一由 DATABASE_SSL／DATABASE_CA_CERT 管理；也拒絕全域停用 TLS 驗證；SEC-17、20 |
| TLS 以 IP 連線時目標名稱檢查不正確 | 即使信任憑證 CA，也必須檢查憑證是否屬於實際連線目標；握手測試重現 IP 與憑證名稱不符卻通過 | 明確以 DATABASE_URL 的主機／IP 驗證名稱；不信任憑證、名稱不符均遭拒，指定正確 CA 與名稱才完成握手；SEC-21 |

另外補上跨站 Fetch Metadata 檢查、重複 Cookie 拒絕、32 KB JSON 上限、不接受壓縮請求，以及 LINE 日誌不記錄群組 ID／訊息。Fetch Metadata 屬於加強防護；先前既有的 Origin 與 CSRF 檢查並未因此被判定可繞過。

密碼雜湊採 scrypt N=16384、r=8、p=5，是 [OWASP 列出的低記憶體參數組合](https://cheatsheetseries.owasp.org/cheatsheets/Password_Storage_Cheat_Sheet.html)。URL 覆蓋 SSL 行為也與 [node-postgres 官方說明](https://node-postgres.com/features/ssl)一致。LINE 非同步處理依據 [Express 4 錯誤處理規則](https://expressjs.com/en/4x/guide/error-handling/)。

## 驗證範圍與結果

| 範圍 | 實測內容 | 結果 |
|---|---|---|
| 身分與權限 | 每個後台讀寫端點：未登入、偽造 Cookie、舊 Token、任意 Bearer、已登出／過期／密碼輪替憑證 | 通過 |
| CSRF／跨站 | 新增、修改、取消、重寄與登出：缺少／錯誤／其他工作階段的 CSRF、錯誤或空白 Origin、錯誤內容格式 | 通過 |
| 工作階段 | 登入換發且撤銷舊憑證、資料庫只保存 Cookie 雜湊、Secure／HttpOnly／SameSite、重複 Cookie | 通過 |
| 注入／個資 | SQL 注入字串、原型污染、越權寫入欄位、路徑與本文 ID 不同、公開空位 API 無個資、靜態檔與編碼路徑穿越 | 通過 |
| 異常與故障 | JSON 格式／大小／壓縮、LINE 簽章錯誤與內容變更、有效簽章異常事件、資料庫斷線安全失敗、不洩漏內部錯誤 | 通過 |
| 限流 | 真實程序替換、兩實例同時計數、IPv6、代理前綴偽造、到期恢復、登入全站限制、重寄數量 | 通過 |
| TLS | URL 參數衝突、不可信憑證、指定 CA、目標主機／IP 名稱、禁止全域停用驗證 | 通過；本機協議模擬端點 |
| 業務規則 | 每時段合計 12 人；20 筆同時預約只接受 12 位；改期競爭、取消釋位、重送去重 | 通過 |
| 使用者要求 | 13 人提示 LINE 聯絡；不自動建立超額單；Email 在前後台新增／編輯皆必填 | 通過 |
| 真正瀏覽器 | 惡意資料顯示為文字；產生的圖片 0、事件屬性 0、彈窗 0；登出回登入頁 | 通過；單一瀏覽器引擎 |
| 套件 | npm audit 對目前鎖定依賴查詢官方漏洞資料庫 | 0 項已知漏洞，涵蓋 87 個依賴計數 |
| 機密資料 | 38 個文字檔的特徵比對及人工檢視 | 未發現符合掃描特徵的機密值；非 Git 歷史／完整高熵掃描 |

SQL 操作已人工檢查參數化；限流表名只由程式內部固定值選擇。通知目的端固定為 Resend／LINE，未提供使用者可指定伺服器 URL 的入口。此版本沒有檔案上傳、檔案解壓縮、使用者模板執行或付款功能；相應攻擊面未列為測試通過。

## 可查閱的紀錄

- [最終 44 組測試輸出](security-evidence/final-tests.txt)：44 通過、0 失敗、0 略過。
- [修補前第一輪](security-evidence/before-fixes.txt)：前 11 組中有 8 組未達預期；部分是加強防護要求，不能直接當成 8 個獨立漏洞。
- [中途複驗](security-evidence/verification-run-2.txt)：保留 TLS 名稱檢查未通過的證據；最終版本已修復。
- [GitHub 上傳前檢查](security-evidence/github-upload-scan.json)：可達提交歷史及待上傳檔案的特徵掃描；測試紀錄中的本機絕對路徑已替換為 `<project>`。
- [套件漏洞結果](security-evidence/npm-audit.json)、[機密掃描範圍](security-evidence/secret-scan.json)、[瀏覽器觀察紀錄](security-evidence/browser-check.json)。
- [安全測試程式](test/security.test.js)、[整合測試](test/integration.test.js)、[單元測試](test/unit.test.js)。

重跑：安裝 Node.js 24、PostgreSQL 與 OpenSSL，執行 `npm ci`，提供**專用測試資料庫**的 `TEST_DATABASE_URL` 後執行 `npm run test:all`。測試建立及刪除隨機獨立 schema，不清空指定資料庫的既有表；必須允許本機監聽及子程序。不得把這些測試直接指向正式資料庫。

## 正式環境已驗證與限制

- 公開 HTTPS、HTTP 轉址、安全標頭、匿名後台阻擋、真正 Secure Cookie、CSRF 及 IP 標頭偽造防護已通過。使用者也自行完成正式密碼登入。
- 資料庫沒有公開 TCP 代理。應用已改用 `booking_app`，沒有超級使用者、建庫、建角色、複製或繞過 RLS 權限；仍擁有應用資料表和建表權供 migration 使用。
- 真正資料庫測試涵蓋 12 人預約、額外 1 人拒絕、兩組 7 人競爭、取消釋位、Email 必填、去重與後台權限。測試用兩筆資料及臨時工作階段已清除，没有發出通知。
- 已做一次手動備份並在隔離資料庫實際還原四張表，演練資料庫已清除。備份檔含工作階段資料，私下保存、不得加入 Git。2026-09-23 使用者自行升級 Pro 後，已啟用每日／每週／每月排程並建立一份原生備份；未對正式資料庫執行還原。
- 2026-09-21 已撤銷先前檢測用 Railway SSH 公鑰。
- Resend 專用網域、受限寄信金鑰及實際收件已驗證；LINE 群組通知尚未配置。沒有對第三方平台做滲透測試。
- 未執行 DDoS／正式高負載測試，現有限流不能保證擋住不同網段的分散式灌單。沒有個人帳號、多因素登入或逐人操作稽核；共用密碼與全站登入上限仍有相應限制。
- Git 機密掃描只涵蓋可達歷史與目前檔案，不代表不可達歷史或曾外流憑證的撤銷狀態已驗證。

公開且已去除私密存取資訊的結果見 [正式驗證紀錄](security-evidence/production-verification.json)。原始檢測歷程保留如下。

## Railway 實測補修（2026-09-14）

首次部署的 31 項公開 HTTPS 驗收中，29 項通過，2 項來源 IP 偽造測試未通過：Railway 沒有依原先本機模擬方式處理 X-Forwarded-For，導致次數限制可被繞過。已將正式 Railway 的預約與登入限制改用平台的 X-Real-IP，缺少或格式錯誤時不退回可偽造標頭。新增 SEC-22 與來源解析測試後，本機 46 項全部通過、27 個 JavaScript 檔案語法檢查通過。修正版已部署，31 項 HTTPS 複驗全數通過，另完成 14 項正式業務／身分驗證測試。原 44 項紀錄保留為首次檢測證據，新結果見 security-evidence/railway-proxy-regression.json。

實際初次 Docker 建置與健康檢查成功；HTTPS、HTTP 301 轉址、HSTS、匿名後台阻擋、Email 必填及 13 人 LINE 提示已實測。原始容器主程序 UID/GID 1000。其後資料庫已改成專用應用角色；備份 API 的 Not Authorized 已由實際頁面確認為 Hobby 方案限制，另完成手動備份還原演練。

## 執行環境漏洞複查（2026-09-21）

同一份 Trivy 資料庫下，原 Debian 12 + Node 完整映像有 4 筆 CRITICAL、59 筆 HIGH、100 筆 MEDIUM、72 筆 LOW、3 筆 UNKNOWN；這些是套件發現項目次數，不等於獨立可利用漏洞數。

正式映像已改為固定 digest 的 Distroless Node.js 24 / Debian 13，不攜帶 npm、yarn 或 shell。基底掃描為 CRITICAL/HIGH 0、MEDIUM 13、LOW 7；剩餘項目來自 libc6、zlib1g，上游套件資料未列修补版本，不能聲稱零漏洞。詳情見 [執行環境掃描](security-evidence/runtime-scan-20260921.json)。npm 鎖定依賴另以最新資料複查，結果為 0。

Docker 建置會在真正執行映像中檢查 Node 24.21 以上、UID 65532、無 shell／套件管理器及四個應用依賴可載入。GitHub Actions 另掃描加入應用依賴後的完整映像；所有高風險與重大項目都阻擋該檢查，不使用 ignore-unfixed 隱藏未修補問題。掃描不能涵蓋所有 Node 二進位內嵌函式庫風險，仍需搭配官方安全版本更新。

### 雲端建置與上線結果

[GitHub Actions 35563516729](https://github.com/GoodTrip2017/chiangmai-booking/actions/runs/35563516729) 在 Node.js 24.21.0 / PostgreSQL 18 的隔離環境通過全部 46 項測試（0 失敗、0 略過），完整應用映像掃描為 HIGH/CRITICAL 0、MEDIUM 13、LOW 7。實際 Node/OpenSSL/UID 分別為 24.21.0 / 3.5.8 / 65532。

Railway 由 GitHub main 自動部署提交 `6f5eb625c246a0a62eddfb1ee1e7d32ab203cabe`；部署 `6750cf6a-ee1b-4904-82e6-ac8021b55003` 為 SUCCESS。Railway 自身的建置紀錄也確認相同執行環境與資料庫健康檢查成功。再次由外部對正式網站執行 31 項檢查，全部通過；這輪沒有建立預約或寄出通知。[雲端驗證摘要](security-evidence/deployment-ci-20260921.json)、[46 項測試輸出](security-evidence/ci-tests-20260921.txt)、[31 項 HTTPS 複驗](security-evidence/production-http-20260921.json)。

2026-09-23 更新：寄件子網域已驗證，新專用金鑰已正確儲存；使用正式寄件設定及應用版型發送的一封驗證信已獲 Resend 接受，使用者確認收到且內容正常。此為本機寄信驗證，不建立預約，也不等同正式預約端到端測試。每日／每週／每月原生備份已啟用，API 保留期為 6／27／89 天，第一份原生備份已列出。證據見 security-evidence/mail-backup-20260923.json。
